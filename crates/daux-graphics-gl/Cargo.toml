[package]
name = "daux-graphics-gl"
description = "OpenGL fallback rendering backend for DAUx plug-in editors."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
daux-graphics = { workspace = true }
glow = { workspace = true }

[dev-dependencies]
# Tests need a `HostServices` to build a `GraphicContext` and drive an editor through its
# lifecycle. Not reachable from this crate's own public API, so it stays a dev-dependency.
daux-host-services = { workspace = true }

[lints]
workspace = true
