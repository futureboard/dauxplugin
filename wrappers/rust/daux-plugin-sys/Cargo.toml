[package]
name = "daux-plugin-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to the DAUx Plugin C ABI"
license = "MIT OR Apache-2.0"

[lib]
# Pure bindings crate; no build artifacts beyond the rlib.

[dependencies]
