#include <DAUx/Factory/PluginFactory.h>

/* Host-side helpers for the modern factory API live in Runtime/PluginFactory.cpp.
 * Entry-point symbols (daux_get_plugin_factory) are exported by plugins only. */
